'use strict';

var transformRequest = function transformRequest(config) {
    var curlString = '';
    console.log(config);
    return curlString;
};

module.exports = {
    transformRequest: transformRequest
};